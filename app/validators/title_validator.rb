class TitleValidator < ActiveModel::Validator

     # def validate(record)
     #      clickbait_titles = ["Won't Believe", "Secret", "Top 10", "Guess"]

     #      unless !clickbait_titles.include?(record.title)
     #           record.errors[:title] << " not clickbait-y enough"
     #      end
     # end

end